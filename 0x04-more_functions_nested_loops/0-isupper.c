#include "main.h"

/**
 * _isupper - Checks if char is an uppercase letter
 * @c: Character to be checked
 * Return: Always 0
 */

int _isupper(int c)
{
	if isupper(c)
		return (1);
	else
		return (0);
}
