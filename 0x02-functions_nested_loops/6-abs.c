#include <stdio.h>
#include <ctype.h>
/**
 * _abs - Returns an integer's absolute value
 * @i: Integer to work with
 * Return: Absolute value of integer
 */

int _abs(int i)
{
	if (i >= 0)
		return (i);
	else
		return (i * -1)
}
